

Interfaces:

Classes / Resources


IntegerWrapper
- Provides a neater development experience by encapsulating a single number and letting the developer call onto the MathExtensions utilities as if they were methods on the number.

	number.GetPrimeDecomposition(); 
		vs
	PrimeDecomposer.GetDecomposition(number);

TODO
- Finish IntergerWrapper